<project name="IJA-Projekt" default="compile" basedir=".">
    <description>Aplikace pro návrh a editaci blokových schémat</description>
    <property name="src" location="src"/>
    <property name="build" location="build"/>
    <property name="src.components" location="src/components"/>
    <property name="build.components" location="build/components"/>
    <property name="src.tests" location="src/tests"/>
    <property name="build.tests" location="build/tests"/>
    <property name="dist" location="dest-client"/>

    <path id="classpath.tests">
        <pathelement location="lib/junit-4.12.jar"/>
        <pathelement location="lib/hamcrest-core-1.3.jar"/>
        <pathelement location="${build.components}"/>
    </path>

    <target name="sources-compile" description="compile the source" depends="clean">
        <mkdir dir="${build.components}"/>
        <javac srcdir="${src.components}" destdir="${build.components}" includeantruntime="false"/>
    </target>
<!--
    <target name="compile" depends="sources-compile" description="create .jar archive">
        <mkdir dir="${dist}"/>
        <jar destfile="${dist}/ija-client.jar"
             basedir="${build}">
            <manifest>
                <attribute name="Main-Class"
                           value="main.Main"/>
            </manifest>
        </jar>
    </target>

    <target name="run" depends="compile">
        <java jar="${dist}/ija-client.jar"
              dir="${dist}"
              fork="true"/>
    </target>
-->
    <target name="compile" depends="sources-compile">
        <mkdir dir="${build.tests}"/>
        <javac srcdir="${src.tests}" destdir="${build.tests}" includeantruntime="false">
            <classpath refid="classpath.tests"/>
        </javac>
    </target>

    <target name="run" depends="clean, compile">
        <junit printsummary="on" haltonfailure="yes" fork="true">
            <classpath>
                <path refid="classpath.tests"/>
                <pathelement location="${build.tests}"/>
            </classpath>
            <formatter type="brief" usefile="false" />
            <batchtest>
                <fileset dir="${src.tests}" includes="**/*.java" />
            </batchtest>
        </junit>
    </target>

    <target name="clean" description="clean up">
        <delete>
            <fileset dir="${dist}" includes="**/*"/>
        </delete>
        <delete includeemptydirs="true">
            <fileset dir="${build}" includes="**/*"/>
        </delete>
    </target>

</project>